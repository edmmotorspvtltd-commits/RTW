# Production Environment Variables
# Rename this file to .env on the server

PORT=3000
NODE_ENV=production

# Database (Ensure this matches setup_vm.sh)
DB_USER=postgres
DB_HOST=localhost
DB_NAME=rtwe_erp
DB_PASSWORD=Shekhar@2506
DB_PORT=5432

# Security (Change these!)
JWT_SECRET=production_secret_key_change_me
SESSION_SECRET=production_session_secret_change_me

# Email Service
SENDGRID_API_KEY=your_sendgrid_key
EMAIL_FROM=noreply@rtwe-erp.com
